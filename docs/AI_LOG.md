# AI LOG

## Task explanation

**Tool used:** Copilot

**Date:** 9 March 2026

**Purpose:** I asked Copilot to explain the meaning of the sentence "Returns true when current path includes the href" because I found the wording confusing.

**Outcome:** It simply explained to me that it means that the function should return true if the link path is contained inside the current URL path, even if the current path is longer. I understood right away what I was suppose to do and went on with my task.

---

## Test structure for getUsername function

**Tool used:** Copilot

**Date:** 9 March 2026

**Purpose:** I wanted recommendations on how to structure tests for the getUsername function, since I had used two describes; one for the localStorage behavior and one for the function itself. I felt it was to much with both and wanted to know what was the best approach.

**Outcome:** The AI suggested first it was best to leave it as I had it since it fulfilled the task. It said this twice until I said I felt like the first describe was unnecessary, and it agreed that I should have it in one if I was only checking getUsername.

---

## Playwright

**Tool used:** Copilot

**Date:** 9 March 2026

**Purpose:** I got some errors one `process.env.TEST_USER_EMAIL` and `process.env.TEST_USER_PASSWORD` and couldn't figure out why.

**Outcome:** Copilot suggested to check ESlint, restart my editor and ensure my files were .js, .mjs or .cjs and included paths. This was not the solution. So I asked some more and went back and forth where it gave me solutions I already had set up. I eventually found the issue myself and removed the //@ts-check |

---

## AI_LOG.md formatting tips

**Tool used:** ChatGPT

**Date:** 12 March 2026

**Purpose:** I wanted to know how to format the AI_LOG.md file to make it more readable and structured. I already had a table setup from my last CA, but prettier for some reason didn't like that setup and made it unreadable.

**Outcome:** I got some examples of how to format the file with section + bullet structure, definition list style and small table + paragraphs. I went with the section + bullet structure since it was the most readable and easy to implement. I also added some horizontal lines to separate the different entries.
